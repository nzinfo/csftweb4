导出表信息
    schema import [-f] app_name [具体的表名]
    1 如果文件已经存在，导入失败
        - 除非使用参数 -f
    2 表结构目前保存为 json 形式，
        - 后续可选 YAML

生成表对应的 Python 代码
    schema genpy app_name
    - 生成对应的 SQLAlchemy 的定义
    - 目前只支持生成在一个大文件中， 后续需要分解为多个文件

[调试]
    - 列出全部的表名
    - 列出表的全部字段名